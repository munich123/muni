pipeline{
    agent any
    stages{
        stage("build"){
            steps{
                sh "pwd"
            }
        }
        stage("crete"){
            steps{
                sh "touch slave"
            }
        }
    }
}
